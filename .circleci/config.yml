version: 2

jobs:
  build:
    docker:
        - image: circleci/node:10.7.0
    steps:
      - run: "false"

workflows:
  version: 2
  build_test_deploy:
    jobs:
      - build:
          filters:
            branches:
              ignore: /.*/
